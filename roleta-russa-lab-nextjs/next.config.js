/** @type {import('next').NextConfig} */
// Configuração mínima de propósito - este é um laboratório de estudo, não
// precisa de otimizações avançadas (rewrites, imagens remotas, etc.).
const nextConfig = {
  reactStrictMode: true,
};

module.exports = nextConfig;
