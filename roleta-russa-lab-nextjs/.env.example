# Aponta pro MESMO MySQL usado pelo docker-compose.yml da raiz do projeto.
# Suba só o banco antes de rodar este laboratório: "docker compose up -d db"
# (rodando esse comando na raiz do projeto, não aqui dentro).
DATABASE_URL="mysql://root:NovaSenhaForte123!@localhost:3307/roleta_russa"
