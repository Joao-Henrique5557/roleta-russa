# Copie este arquivo para ".env" (mesma pasta) só se quiser trocar a senha
# padrão do MySQL. Não precisa mexer em mais nada - o docker-compose.yml já
# cuida do resto (o MySQL roda containerizado, não precisa instalar nada
# na sua máquina).
DB_PASSWORD=NovaSenhaForte123!
